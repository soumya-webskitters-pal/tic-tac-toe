import GameBoard from './GameBoard'
import ScoreBoard from './ScoreBoard'
export default function GameArena({ game, onNewMatch, onPlayFriends }) { return <div className="arena"><div className="arena-toolbar"><button className="board-friends-trigger" onClick={onPlayFriends}><span>♟♟</span> PLAY WITH FRIENDS</button><button className="hint-trigger" onClick={game.showHint} disabled={game.thinking || game.result || (game.mode === 'ai' && game.turn === game.aiSymbol)} title="Suggest the best move"><span className="bulb-icon">💡</span> HINT</button><button className="setup-trigger" onClick={onNewMatch}>⚙ <span>NEW MATCH</span></button></div><div className="status-row"><span className={`pulse ${game.turn.toLowerCase()}`} /><strong>{game.status}</strong>{game.mode==='online'&&game.onlineStatus==='playing'?<span className={`network-signal ${game.networkQuality}`} title={`${game.networkQuality} connection`}><i/><i/><i/><em>{game.networkQuality}</em></span>:<small>{game.result ? 'ROUND COMPLETE' : `${game.size} × ${game.size} GRID`}</small>}</div><GameBoard game={game}/><ScoreBoard game={game}/></div> }
