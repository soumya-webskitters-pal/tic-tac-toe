import EditableName from './EditableName'
import Icon from './Icon'
export default function GameControls({ game, onStart }) {
  const { mode,setMode,difficulty,setDifficulty,size,setSize,playerSymbol,setPlayerSymbol,names,rename,newMatch } = game
  return <div className="controls"><div className="control-section"><label>GAME MODE</label><div className="segmented"><button className={mode === 'ai' ? 'selected' : ''} onClick={() => setMode('ai')}><Icon>✦</Icon> VS AI</button><button className={mode === 'pvp' ? 'selected' : ''} onClick={() => setMode('pvp')}><Icon>♟</Icon> 2 PLAYERS</button></div></div><div className="control-section"><label>{mode === 'ai' ? 'CHOOSE YOUR MARK' : 'PLAYER ONE MARK'}</label><div className="symbol-choice"><button className={playerSymbol === 'X' ? 'selected x' : 'x'} onClick={() => setPlayerSymbol('X')}><b>X</b><span>PLAY AS X</span></button><button className={playerSymbol === 'O' ? 'selected o' : 'o'} onClick={() => setPlayerSymbol('O')}><b>O</b><span>PLAY AS O</span></button></div><p className="hint random-start">✦ The first round starts randomly; opening turns alternate after that.</p></div>{mode === 'ai' && <div className="control-section appear"><label>AI INTELLIGENCE</label><div className="difficulty-cards"><button className={difficulty === 'easy' ? 'selected' : ''} onClick={() => setDifficulty('easy')}><span>CASUAL</span><small>Relaxed & unpredictable</small><i /></button><button className={difficulty === 'hard' ? 'selected' : ''} onClick={() => setDifficulty('hard')}><span>TACTICAL <b>PRO</b></span><small>Calculates the best move</small><i /></button></div></div>}<div className="control-section"><label>GRID SIZE</label><div className="size-row">{[3,4,5,6].map(n => <button key={n} className={size === n ? 'selected' : ''} onClick={() => setSize(n)}>{n}×{n}</button>)}</div><p className="hint">Connect {size === 3 ? 3 : 4} marks in a row to win.</p></div><div className="players"><EditableName value={names.X} symbol="X" onChange={v => rename('X',v)} /><div className="versus">VS</div><EditableName value={names.O} symbol="O" muted={mode === 'ai'} onChange={v => rename('O',v)} /></div><button className="new-game" onClick={() => { newMatch(); onStart() }}><span>✦</span> START NEW MATCH</button></div>
}
